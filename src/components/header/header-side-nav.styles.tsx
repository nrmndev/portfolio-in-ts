import styled from "styled-components";

const StyledSideNav = styled.ul`
  margin: 0;
  padding: 0;
  z-index: 20;
  top: calc(50% - 50px);
  left: 0;
  position: fixed;
  width: auto;
  height: auto;
`;

export default StyledSideNav;
